# Email delivery (form handler → Resend). Copy to .env.local and fill in.
RESEND_API_KEY=
# Address that receives new lead notifications
LEAD_NOTIFICATION_TO=
# Verified "from" address on your Resend domain
LEAD_NOTIFICATION_FROM=
